/*Write a for loop that prints a simple text pattern. For instance, printing a pyramid of asterisks (*) 
where each level has one more asterisk than the level above it, up to 5 levels.*/

for (let sign = "*"; sign.length <= 5; sign += "*"){
    console.log(sign);
} 
